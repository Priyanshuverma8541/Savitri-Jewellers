<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body
    <p>Edit your Chat</p>
    <p>
        This chat was sent by <b><%= chat.from %></b> to <b> <%= chat.to %> </b>
    </p>
    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
        <textarea name="msg" rows="16" cols="40" id=""><%= chat.msg %></textarea>
        <button>Edit</button>
    </form>
    
</body>
</html>