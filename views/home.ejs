<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat App</title>
    <style>
        /* Global styles */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(135deg, #b7ea8e, #1d599e);
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }

        /* Card container */
        .chat-container {
            background: rgba(16, 125, 161, 0.1);
            border-radius: 16px;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
            padding: 20px;
            text-align: center;
            max-width: 400px;
            width: 100%;
        }

        /* Header styling */
        .chat-container h1 {
            font-size: 2rem;
            margin-bottom: 10px;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.3);
        }

        /* Paragraph styling */
        .chat-container p {
            font-size: 1rem;
            margin: 10px 0 20px;
            line-height: 1.5;
            color: rgba(255, 255, 255, 0.8);
        }

        /* Button styling */
        .chat-container button {
            background: #7aced1;
            color: #fff;
            border: none;
            padding: 10px 20px;
            font-size: 1rem;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 10px rgba(255, 126, 179, 0.3);
        }

        /* Button hover effect */
        .chat-container button:hover {
            background: #ff5c99;
            box-shadow: 0 6px 15px rgba(255, 92, 153, 0.4);
            transform: translateY(-3px);
        }

        /* Button active effect */
        .chat-container button:active {
            transform: translateY(1px);
            box-shadow: 0 2px 5px rgba(255, 92, 153, 0.2);
        }

        /* Subtle animations */
        .chat-container {
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.9);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }
    </style>
</head>
<body>
    <div class="chat-container">
        <h1>Welcome to ChatApp</h1>
        <p>Stay connected with your friends. Start chatting and keep track of all your conversations seamlessly.</p>
        <form method="GET" action="/chats">
            <button type="submit">Start Chatting</button>
        </form>
    </div>
</body>
</html>
